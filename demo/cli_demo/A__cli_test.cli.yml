steps:
  - cli: snow
    command: init
    working_dir: .
    description: "Test CLI"
  - cli: snow
    command: sql
    args:
      - "--connection"
      - "defaultold"
      - "--query"
      - "SELECT 1"
    working_dir: .
    description: "Test CLI Query"
